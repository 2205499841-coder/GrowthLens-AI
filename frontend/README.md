# GrowthLens Frontend

## 本地运行

```bash
pnpm install
pnpm dev
```

打开 <http://localhost:3000>。

复制环境变量示例：

```bash
cp .env.local.example .env.local
```

上传功能默认调用 <http://localhost:8000/api/uploads/parse>。

## 检查

```bash
pnpm lint
pnpm build
```
