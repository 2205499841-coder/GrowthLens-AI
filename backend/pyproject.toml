[project]
name = "growthlens-backend"
version = "0.1.0"
description = "GrowthLens AI Excel analysis API"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
